		<% reply = Reply.where(:comment_id => comment_id) %>
		<% loopCounter = 0; %>

		<% reply.each do |r| %>
			
	  		
	  		<% reply = Reply.find(r.id) %>
	  		<li>
							<div class="commentTop">
								<ul>

									<li><a href="#"><%= User.find(reply.user_id).username %></a></li>
									<li><% from_time = Time.now %><%= distance_of_time_in_words(reply.created_at, from_time) %> ago</li>
									<li class="NestedReply"><a href="#">Reply</a></li>
								</ul>
								<div class="clear"></div>
							</div>
							
							<%= reply.body %>
							

							<div class="reply">
								<ul>
									
									<%= render :partial => 'nestedreply', :locals => { :comment_id => comment_id, :parent_id => reply.parent_reply_id } %>

								</ul>
							</div>
							
							
							
						</li>


	  	<% end %>
	  	