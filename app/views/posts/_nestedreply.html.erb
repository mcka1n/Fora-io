		<% reply = Reply.where(:comment_id => comment_id, :parent_reply_id => parent_id) %>
		

	  	<% if reply[0].nil? || parent_id == 0 %>
	 


	  	<% else %>
	  		
	  		<% reply = Reply.find(reply[0].id) %>
	  		
						<li>
							<div class="commentTop">
								<ul>
									
									<li><a href="#"><%= User.find(reply.user_id).username %></a></li>
									<li><% from_time = Time.now %><%= distance_of_time_in_words(reply.created_at, from_time) %> ago</li>
									<li class="NestedReply"><a href="#">Reply</a></li>
								</ul>
								<div class="clear"></div>
							</div>
							<div class="cont">
								<%= reply.body %>
							</div>							
							
						</li>
			
	  	<% end %>